'use strict';

module.exports = {
  showConfirmationPrompt: function showConfirmationPrompt(message) {
    var defaultValue = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;

    var _require = require('inquirer'),
        createPromptModule = _require.createPromptModule;

    return createPromptModule()([{
      type: 'confirm',
      name: 'answer',
      message: message,
      "default": defaultValue
    }]).then(function (input) {
      return input.answer;
    });
  }
};