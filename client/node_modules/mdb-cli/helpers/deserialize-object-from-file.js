'use strict';

var fs = require('fs');

module.exports = {
  deserializeJsonFile: function deserializeJsonFile(filePath) {
    return new Promise(function (resolve, reject) {
      fs.readFile(filePath, 'utf8', function (error, content) {
        error ? reject(error) : resolve(JSON.parse(content));
      });
    });
  }
};