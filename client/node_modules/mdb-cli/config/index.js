'use strict';

var homedir = require('os').homedir();

var env = 'dev';
module.exports = {
  tokenDir: "".concat(homedir, "/.mdbcli"),
  tokenFile: '/.auth',
  env: env,
  port: process.env.PORT || 3030,
  host: process.env.HOST || 'localhost'
};